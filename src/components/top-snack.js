import React from "react";

const TopSnackBar = () => {
  return (
    <div className="text-center bg-blue-700 py-3 text-white w-full">
      Clinic is now open <span className="font-semibold">SUNDAY</span> as well !
    </div>
  );
};

export default TopSnackBar;
